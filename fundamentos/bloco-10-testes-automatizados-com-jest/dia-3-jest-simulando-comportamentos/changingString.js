const firstFunction = str => str.toUpperCase();

const secondFunction = str => str[0];

const thirdFunction = (a, b) => `${a}${b}`;

module.exports = {
  firstFunction,
  secondFunction,
  thirdFunction,
};